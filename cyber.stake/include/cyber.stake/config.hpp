#pragma once

namespace cyber { namespace config {
static const auto reward_memo = "reward";
    
// permissions
static const auto amerce_name = "amerce"_n;
static const auto reward_name = "reward"_n;
}

} // cyber::config
